ai:
  mode: mock
  mock:
    enabled: true

spring:
  ai:
    openai:
      enabled: false
  autoconfigure:
    exclude: org.springframework.ai.autoconfigure.openai.OpenAiAutoConfiguration,org.springframework.ai.autoconfigure.chat.client.ChatClientAutoConfiguration

